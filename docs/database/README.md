# Database Documentation

This directory documents Falcon's SQLite data model, query patterns, migrations,
full-text search behavior, and dataset build process.

Database documentation should explain schema intent, provenance expectations, and
data lifecycle rules without requiring contributors to inspect generated database
artifacts directly.

